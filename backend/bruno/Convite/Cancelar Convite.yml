info:
  name: Cancelar Convite
  type: http
  seq: 3

http:
  method: delete
  url: http://localhost:3000/estudos/:estudo_id/convites/:convite_id
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: estudo_id
      value: "1"
      type: path
    - name: convite_id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
