info:
  name: Enviar Convite
  type: http
  seq: 1

http:
  method: post
  url: http://localhost:3000/estudos/:estudo_id/convites
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: estudo_id
      value: "32"
      type: path
  body:
    type: json
    data: |-
      {
        "email_convidado": "chrisnotads2020@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
