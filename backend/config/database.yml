# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: <%= ENV.fetch("POSTGRES_HOST") { "localhost" } %>
  port: <%= ENV.fetch("POSTGRES_PORT") { 5410 } %>
  username: <%= ENV.fetch("POSTGRES_USER") { "root" } %>
  password: <%= ENV["POSTGRES_PASSWORD"] %>

development:
  primary:
    <<: *default
    database: <%= ENV.fetch("POSTGRES_DB") { "kheprix_oltp_db" } %>
    host: <%= ENV.fetch("POSTGRES_HOST") { "db" } %> # "db" é o nome no docker-compose

  dw:
    <<: *default
    database: <%= ENV.fetch("POSTGRES_DW_DB") { "kheprix_dw_db" } %>
    username: <%= ENV.fetch("POSTGRES_DW_USER") { "kheprix_dw_user" } %>
    password: <%= ENV.fetch("POSTGRES_DW_PASSWORD") { "senha_analitica" } %>
    host: <%= ENV.fetch("POSTGRES_DW_HOST") { "db_dw" } %> # "db_dw" é o nome no docker-compose
    port: 5432
    migrations_paths: db/dw_migrate
    schema_search_path: public, staging
    database_tasks: false


# As with config/credentials.yml, you never want to store sensitive information,
# like your database password, in your source code. If your source code is
# ever seen by anyone, they now have access to your database.
#
# Instead, provide the password or a full connection URL as an environment
# variable when you boot the app. For example:
#
#   DATABASE_URL="postgres://myuser:mypass@localhost/somedatabase"
#
# If the connection URL is provided in the special DATABASE_URL environment
# variable, Rails will automatically merge its configuration values on top of
# the values provided in this file. Alternatively, you can specify a connection
# URL environment variable explicitly:
#
#   production:
#     url: <%= ENV["MY_APP_DATABASE_URL"] %>
#
# Read https://guides.rubyonrails.org/configuring.html#configuring-a-database
# for a full overview on how database connection configuration can be specified.
#
test:
  primary:
    <<: *default
    database: kheprix_test_db
    host: <%= ENV.fetch("POSTGRES_HOST") { "db" } %>

production:
  primary: &primary_production
    <<: *default
    database: backend_production
    username: backend
    password: <%= ENV["BACKEND_DATABASE_PASSWORD"] %>
  cache:
    <<: *primary_production
    database: backend_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: backend_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: backend_production_cable
    migrations_paths: db/cable_migrate
